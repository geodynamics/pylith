# Parameters for mesh.
#
#{Units('si')}
#
# ----------------------------------------------------------------------
# Parameters for geometry.
# ----------------------------------------------------------------------
#
# Block is 10.0 km x 10.0 km x 10.0 km
# -5.0 km <= x <= 5.0 km
# -5.0 km <= y <= 5.0 km
# -10.0 km <= z <= 0 km
#
#{blockWidth=10*km}
#{blockLength=10*km}
#{blockHeight=10*km}
#
# ----------------------------------------------------------------------
# Parameters for mesh.
# ----------------------------------------------------------------------
#
#{dx=5.0*km}
