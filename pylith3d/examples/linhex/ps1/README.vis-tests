This is a description of the viscous prestress tests:

1.  No prestresses, no material property differences.
2.  Prestress autocomputation using original material properties,
    no material property differences.
3.  Prestress autocomputation using tpois = 0.4999, no bbar,
    no material property differences.
4.  Prestress autocomputation using tpois = 0.4999, bbar,
    no material property differences.
5.  No prestresses, rho2=0.5*rho1, no other property differences.
6.  Prestress autocomputation using original material properties,
    rho2=0.5*rho1, no other property differences.
7.  Prestress autocomputation using tpois = 0.4999, no bbar,
    rho2=0.5*rho1, no other property differences.
8.  Prestress autocomputation using tpois = 0.4999, bbar,
    rho2=0.5*rho1, no other property differences.
9.  No prestresses, rho2=0.5*rho1, vis2=10.0*vis1.
10. Prestress autocomputation using original material properties,
    rho2=0.5*rho1, vis2=10.0*vis1.
11. Prestress autocomputation using tpois = 0.4999, no bbar,
    rho2=0.5*rho1, vis2=10.0*vis1.
12. Prestress autocomputation using tpois = 0.4999, bbar,
    rho2=0.5*rho1, vis2=10.0*vis1.
